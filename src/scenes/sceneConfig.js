export const sceneConfigs = {
  main: {
    sceneName: "main",
    mapFile: "/map.json",
    mapSprite: "map",
    scale: 2,
    cameraOffset: -100,
    doorTransitions: {
      // Door transitions removed - single scene portfolio
    },
    spawnPoints: {
      // Default spawn points
    }
  }
};